# Librerías Python por categoría

## Datos tabulares
`pandas`, `polars`, `duckdb`, `pyarrow`, `numpy`.

## Extracción
`requests`, `httpx`, `beautifulsoup4`, `lxml`, `playwright`, `pdfplumber`, `camelot`.

## APIs específicas
`wbgapi` (World Bank), `eurostat`, `sentinelhub`, `pystac-client`, `openeo`.

## Geoespacial
`geopandas`, `shapely`, `pyproj`, `h3`, `rasterio`, `rioxarray`, `xarray`,
`folium`, `keplergl`, `pydeck`.

## Estadística y ML
`scipy`, `statsmodels`, `scikit-learn`, `xgboost`, `lightgbm`, `shap`.

## Series temporales
`statsmodels`, `prophet`, `sktime`, `pmdarima`, `darts`.

## Visualización
`matplotlib`, `plotly`, `altair`, `seaborn`.

## Calidad
`pandera`, `great_expectations`.

## Apps y APIs
`streamlit`, `fastapi`, `uvicorn`, `pydantic`.

## Tooling
`ruff`, `black`, `sqlfluff`, `pre-commit`, `uv`, `poetry`, `jupytext`, `quarto`.
